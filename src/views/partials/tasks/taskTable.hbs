<table>
    <thead>
        <tr>
            <th>Nº</th>
            <th>Title</th>
            <th>Description</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        {{#each tasks}}
        <tr>
            <td>{{@index}} </td>
            <td>{{title}}</td>
            <td>{{description}}</td>
            <td>
                {{#if done}}
                <a href="/tasks/{{_id}}/toogleDone"><button>Undone</button></a>
                {{else}}
                <a href="/tasks/{{_id}}/toogleDone"><button>Done</button></a>
                {{/if}}
                <a href="/tasks/{{_id}}/edit"><button>Edit</button></a>
                <a href="/tasks/{{_id}}/delete"><button>Delete</button></a>

            </td>
        </tr>
        {{/each}}
    </tbody>
</table>